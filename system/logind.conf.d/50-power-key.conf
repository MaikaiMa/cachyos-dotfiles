# Installed as /etc/systemd/logind.conf.d/50-power-key.conf by
# scripts/setup-power-key.sh. niri's power-key handling is disabled for
# niri-wm/niri#2233, so logind suspends on a short press instead.
[Login]
HandlePowerKey=suspend
