# Official Arch/CachyOS packages only.
#
# Baseline: a CachyOS installation with the "Niri / Noctalia" desktop profile,
# which installs cachyos-niri-noctalia. This manifest records only what the
# managed configuration, helpers, validation, and documented setup need on top
# of or from within that baseline. Kernel, bootloader, driver, filesystem, and
# other installer-owned packages are deliberately not recorded; see ADR-0009.
#
# One package per line; an inline comment may follow. Compare this manifest
# with the machine using scripts/check-packages.sh.

# Repository checkout, deployment, and validation.
git
chezmoi
fish
shellcheck
shfmt
diffutils

# Desktop session from the CachyOS profile, referenced directly by the config.
cachyos-niri-noctalia
niri
noctalia
capitaine-cursors # cursor theme in niri misc.kdl

# Managed desktop configuration and helpers.
playerctl # dashboard media state and controls
power-profiles-daemon # dashboard power profiles
protonmail-bridge-core # enabled user service
jq # focus-or-spawn, audio glow, dashboard state
util-linux # flock in focus-or-spawn
imagemagick # dashboard wallpaper preview
noto-fonts # Noto Sans Medium in the wallpaper preview

# Applications launched by managed keybinds or set as default handlers.
alacritty
zed
nautilus
zen-browser-bin
chatgpt-desktop-bin
github-cli # git credential helper in .gitconfig

# Mail: Flatpak runtime for Hylki and GNOME Online Accounts for Google OAuth.
flatpak
gnome-online-accounts
gnome-control-center # only UI that can add GNOME Online Accounts

# CachyOS gaming stack; equivalent to "Install Gaming packages" in CachyOS Hello.
cachyos-gaming-meta
cachyos-gaming-applications

# CachyOS AUR helper used to install the AUR manifest.
paru

# Dutch + US English spelling; Enchant exposes dictionaries to supporting apps.
hunspell
hunspell-nl
hunspell-en_us
enchant
