# The packaged unit is written for gnome-session; tie squeekboard to niri so
# it starts once the Wayland socket exists and stops with the session.
[Unit]
PartOf=niri.service
After=niri.service

[Service]
RestartSec=1
